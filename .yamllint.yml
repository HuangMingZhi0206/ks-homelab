extends: default

ignore: |
  backups/
  traefik/acme/

rules:
  line-length: disable
  document-start: disable
  comments:
    min-spaces-from-content: 1
  truthy:
    check-keys: false
  indentation:
    spaces: 2
    indent-sequences: whatever
